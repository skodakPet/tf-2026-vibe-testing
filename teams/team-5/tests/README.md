# Your team's tests

Put your `*.spec.ts` files here. Name the story in each test, for example
`test('FD-05 · cart total includes the delivery fee', …)`.

Use relative paths — `page.goto('/')` — never the app's address. The config supplies it.

```bash
cd teams/team-N
npx playwright test
```
